import { StyleSheet, ViewStyle } from "react-native";

const App: ViewStyle = {
  flex: 1,
  justifyContent: "center",
  alignItems: "center",
  backgroundColor: "#F5FCFF",
  padding: 20
};

export const Styles = StyleSheet.create({ App });
